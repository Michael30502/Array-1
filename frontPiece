public int[] frontPiece(int[] nums) {
  
int check = 2;
if(nums.length <2)
check = nums.length;

  int[] answer=new int[check];

  
  for(int i = 0;i<nums.length;i+=1){
    if (i==2)
      break;
      answer[i] = nums[i]; 
    
  }
  return answer;
}
